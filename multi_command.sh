#!/bin/bash
mkdir bin
./bin/rshell
pwd;ls;echo"hello"
        ls;    pwd       ;
            ls&& cd && ls && -a
                ls || cd || ls
cd || ls ||pwd
git log || ls
git log && ls
git log || -a
 git log && -a || ls
ls||ls
ls | ls
ls&&pwd
sl||ls
